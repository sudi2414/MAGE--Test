# MAGE--Test
mage-test is a multi-agent LLM pipeline that automatically generates, inspects, reviews, and curates high-quality JUnit5 unit tests for Java source code. It uses a team of specialized AI agents — each with a distinct role — orchestrated to produce tests that are syntactically correct, semantically sound, and coverage-optimized.
